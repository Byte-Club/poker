require('../poker');

Poker.Model = DS.Model.extend({
	//TODO: Get validations working
	//init: function(){
		//var validation;

		//for(var key in this.validates){
			//validation = this.validates[key];
			//this[key] = DS.attr(validation.type, validation.options);
		//}
		//this._super();
	//}
});
